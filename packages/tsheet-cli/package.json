{
  "name": "tsheet-cli",
  "version": "0.0.0",
  "private": true,
  "description": "tsheet の検証・形式変換・export・Git 連携を行う CLI",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p . && tsc --noEmit -p test"
  },
  "dependencies": {
    "tsheet-core": "workspace:*"
  }
}
