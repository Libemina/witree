# セキュリティポリシー

> **Summary (English):** Please report security vulnerabilities through GitHub's [Private vulnerability reporting](https://github.com/Libemina/witree/security/advisories/new), not through public issues, pull requests, or discussions. While v0.1 is under development there are no released versions, and only the `main` branch is supported. This is a single-maintainer project: reports are handled on a best-effort basis, and there is no bug bounty.

## 脆弱性の報告

脆弱性を見つけた場合は、GitHub の **Private vulnerability reporting** で報告してください。

- 報告窓口：<https://github.com/Libemina/witree/security/advisories/new>
  （リポジトリの **Security** タブ → **Report a vulnerability** からも開けます）

**公開の Issue・Pull Request・Discussions には脆弱性の内容を書かないでください。** 修正が用意できる前に詳細が公開されると、利用者を危険にさらすおそれがあります。脆弱性かどうか判断がつかない場合も、まずは上記の窓口から非公開でお知らせください。

報告は日本語・英語のどちらでも構いません。

### 報告に含めてほしい内容

- 問題の概要と、想定される影響（何ができてしまうか）
- 対象（`tsheet-core` / `tsheet-cli` / `witree` / 仕様書 / CI など）と、確認したコミットのハッシュ
- 再現手順。可能であれば、再現できる最小のワークブック（`.tsheet` またはフォルダ形式）や式、コマンド
- 実行環境（OS、Node.js またはブラウザの種類と版）
- 修正や回避策の案があれば、その内容

### 報告後の流れ

このプロジェクトはメンテナーが 1 人で運営しています。対応期限は約束できませんが、できる範囲で速やかに次のとおり対応します。

1. 報告を確認し、受領した旨をアドバイザリ上で返信します。
2. 内容を調査し、脆弱性と判断した場合は非公開のまま修正を進めます。必要に応じて追加の情報をお尋ねします。
3. 修正を `main` に反映した後、GitHub Security Advisory として公開します。希望される場合は、報告者としてお名前を記載します。

報奨金（バグバウンティ）の制度はありません。

## サポート対象のバージョン

現在は仕様 v0.1（ドラフト）の開発中で、リリース済みのバージョンはありません。サポート対象は **`main` ブランチの最新のコミットのみ** です。

| 対象 | サポート |
|---|---|
| `main` ブランチ（最新） | 対象 |
| その他のブランチ・過去のコミット | 対象外 |

修正は `main` にのみ行います。最初のリリース後は、この節をリリースに合わせて更新します。

## 対象範囲

Witree は、他者が作成したワークブックを開くことを前提にしています。そのため、**信頼できないワークブックを読み込んだとき** に起こる次のような問題は、脆弱性として扱います。

- **パスの扱い** — 仕様では、単一ファイル形式の `%%part <path>` は `..` を含んではならず（[形式仕様](spec/tsheet-spec-v0.1.md) §2.2）、`doc` 型の本文のパスは `docsDir` 配下に限られ `..` を含んではなりません（同 §6.5）。これをすり抜けて、`unpack` や保存の際にワークブックの外のファイルを読み書きできてしまう問題。
- **式と正規表現によるサービス拒否** — 式は副作用も I/O も持たない宣言的な言語で、リスクは過大な計算に限られます。エンジンは、UI から渡された式にも、ファイルから読み込んだ式にも、同じ制限（`ExprLimits`：式の長さ、構文木の深さ、演算ステップ数、正規表現のパターン長、文字列演算の結果長）を課し、超えた場合は評価を打ち切って `E20` を返します。また、`pattern`・`REGEXMATCH` の正規表現では、ネストした量指定子を拒否します（[エンジン API 仕様](spec/tsheet-engine-api-v0.1.md) §9）。これらの制限をすり抜けて、エンジンを停止させたり、極端に長い時間応答しなくさせたりできる問題（ReDoS を含む）。
- **式からの外部アクセス** — 式の評価を通じて、ファイル・ネットワーク・実行環境の情報にアクセスできてしまう問題。
- **デスクトップ版のファイルアクセス** — デスクトップ版（Tauri）の Rust 側は、ファイル入出力・変更監視・アトミック保存を担います。利用者が開いたワークブック以外のファイルを、ワークブックの内容によって読み書きさせられる問題。
- **UI での表示** — ワークブック内の値（ラベル、`doc` 型の本文など）を表示する際に、スクリプトが実行されてしまう問題。
- **CI とサプライチェーン** — このリポジトリの GitHub Actions ワークフローや依存関係の設定の不備により、リポジトリやビルド成果物が改ざんされうる問題。

実装は開発の途中であり、上記の仕様上の防御がまだ実装されていない部分もあります。未実装の機能に関する指摘は、通常の Issue でお寄せください。実装済みの部分が仕様どおりに防御できていない場合は、脆弱性として報告してください。

### 対象外

- 利用者自身が、自分の権限で、自分のファイルを操作することで起こる結果（例：自分で書いた重い式により自分の環境で計算が遅くなる。ただし `ExprLimits` の範囲内であること）
- 依存パッケージの既知の脆弱性で、このプロジェクトでの利用方法では影響が生じないもの（影響が生じる場合は対象です）
- 値の違反を含むワークブックを受理すること。tsheet は、値の違反は受理して診断で知らせ、構造の違反だけを拒否する設計です
- セキュリティ上の影響を伴わない不具合や仕様への提案（通常の [Issue](https://github.com/Libemina/witree/issues) でお寄せください）
